---
# file: general.yml

- hosts: localhost
  roles:
    - { role: brew-cask, when: ansible_os_family == "Darwin" and ansible_distribution == "MacOSX" }
    - packagemanagers
    - ansible
    - git
    - zsh
    - ruby
    - vim
    - nodejs
    - debug

